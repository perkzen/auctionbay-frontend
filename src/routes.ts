export enum PublicRoute {
  HOME = '/',
  SIGNUP = '/signup',
  LOGIN = '/login',
  FORGOT_PASSWORD = '/forgot-password',
}

export enum PrivateRoute {
  AUCTIONS = '/auctions',
  PROFILE = '/profile',
}
